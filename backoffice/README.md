# Instalação e Execução

Primeiro instale as dependencias:

```
npm install
```

Depois, você pode executar em modo desenvolvimento:

```bash
npm run dev
```

Abra o navegador na seguinte rota [http://localhost:3000](http://localhost:3000).
